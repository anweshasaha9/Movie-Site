<?php
/**
 * Implements hook_page_attachments_alter
 */
function daisy_page_attachments_alter(array &$page) {
  // If homepage, attach library.
  $isFront = \Drupal::service('path.matcher')->isFrontPage();
  if ($isFront) {
    $page['#attached']['library'][] = 'daisy/homepage';
  }
}
